# Day28 - Advanced Networking with VPCs and Peering

 ## * Ansible - Set Up VPC Peering Between AWS Accounts

  ```yaml
          - name: Setup VPC Peering Between Accounts
            hosts: localhost
            tasks:
               - name: Create VPC peering connection
                command: aws ec2 create-vpc-peering-connection --vpc-12345678 --peer-vpc-id vpc-87654321
 * terraform- VPC Peering and Route Tables
    resource "aws_vpc_peering_connection" "pahtnex_peering" {
       vpc_id    = aws_vpc.pathnex_vpc.id
       peer_vpc_id = aws_vpc_peer_vpc.id
    }

    resource "aws_route" " pathnex_route" {
      route_table_id        =aws_route_table.pahtnex_rt.id
      destination_cidr_block = "10.0.0.0/16"
      vpc_peering_connection_id = aws_vpc_peering_coneection.pathnex_peering.id
     }
      
 * Kubernetes - Multi-Cluster VPC Peering and Network Policy
      apiVersion: networking.k8.io/v1
      kind: NetworkPolicy
      metadata:
         name: pathnex-vpc-poicy
       spec:
         podSelector:
          matchLabels:
           app: pathnex-app
          ingress:
          - from:
            -podSelector:
              matchLabels:
              app: pathnex-db

    * Docker
  # Docker Save, Load & Backup
    docker save -o pathnex-nginx.tar pathnex-nginx
    docker load -i pathnex-nginx.tar

    # bash
     docker run --rm -v pathnex-volume:/data -v $(pwd):/backup ubuntu tar czf /backup pathnex-backup.tar.gz /data


     
    
